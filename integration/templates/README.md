# Templates

HTML templates are source files. They are rendered into `out/` (search report into `out/search/`, operator SRAM report into `out/sram/`), so relative links inside a template are written relative to the rendered report, not to the template. Do not hand-edit generated HTML.
