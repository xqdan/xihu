/* B=1 only search with direct RDMA-to-remote-SRAM memory semantics. */
'use strict';
const fs=require('fs'),crypto=require('crypto');
const A=require('../search/k3_architecture_search.js'),R=require('./k3_sram_memory_rdma_model');
const TARGET=1000,MARGIN=1.17;
const EXT={...A.SPACE,nL:[4,8,12,16,24,32],nH:[4,8,12,16,24,32],lRows:[1,2,4,8,16],lCols:[64,128,256],lEngines:[1,2,4,8],vectorLanes:[128,256,512,1024,2048],reduceLanes:[128,256,512,1024,2048,4096],kvTile:[4096,8192,16384,32768],headTile:[16,32,48,96]};
function evaluate(x,detail=false){const r=R.evaluate(x,{detail});if(!r.feasible)return r;return {...r,tokensPerSecond:r.tps};}
function search(space,seeds,{initial,generations,offspring,polish,seed}){const rand=A.rng(seed),pick=a=>a[Math.floor(rand()*a.length)],keys=Object.keys(space),seen=new Set(),rows=[],rejects={},progress=[];let attempts=0;
 const test=z=>{const key=keys.map(k=>z[k]).join('|');if(seen.has(key))return;seen.add(key);attempts++;const r=evaluate(z);if(!r.feasible){for(const k of r.reasons||[r.reason])rejects[k]=(rejects[k]||0)+1;return;}r.id=rows.length;rows.push(r);if(rows.length%100===0)console.log('RDMA-SRAM feasible',rows.length,'best',Math.max(...rows.map(v=>v.tps)).toFixed(2));};
 const ordered=()=>rows.slice().sort((a,b)=>b.tps-a.tps||a.p.dieArea-b.p.dieArea||a.p.cardPower-b.p.cardPower);
 for(const seedx of seeds){const z={};for(const k of keys)z[k]=space[k].reduce((a,b)=>Math.abs(a-seedx[k])<=Math.abs(b-seedx[k])?a:b);test(z);}
 let tries=0;while(rows.length<initial&&tries++<initial*1000)test(Object.fromEntries(keys.map(k=>[k,pick(space[k])] )));if(!rows.length)throw Error('No feasible design');progress.push({phase:'init',count:rows.length,tps:ordered()[0].tps});
 for(let g=0;g<generations;g++){const elite=ordered().slice(0,12),target=rows.length+offspring;tries=0;while(rows.length<target&&tries++<offspring*1000){const z={...pick(elite).x},other=pick(elite).x;if(rand()<.3)for(const k of keys)if(rand()<.5)z[k]=other[k];for(let i=0,n=1+Math.floor(rand()*5);i<n;i++){const k=pick(keys);z[k]=pick(space[k]);}test(z);}progress.push({phase:'gen '+(g+1),count:rows.length,tps:ordered()[0].tps});}
 for(let pass=0;pass<polish;pass++){for(const r of ordered().slice(0,2))for(const k of keys)for(const v of space[k])if(v!==r.x[k])test({...r.x,[k]:v});progress.push({phase:'local '+(pass+1),count:rows.length,tps:ordered()[0].tps});}
 const best=ordered()[0],qualified=rows.filter(r=>r.tps>=TARGET).sort((a,b)=>a.p.dieArea-b.p.dieArea||a.p.cardPower-b.p.cardPower);return {space,options:{initial,generations,offspring,polish,seed},attempts,rejects,progress,qualified:qualified.length,best:evaluate(best.x,true),targetCandidate:qualified[0]?evaluate(qualified[0].x,true):null,rows};}
function diagnostics(best){const m=R.mapped(best.x),comm=m.plan.ops.filter(o=>o.unit==='COMM');const protocols=best.protocol||[];return {collectives:comm.length,phases:best.phases,requests:best.requests,wireBytes:best.wireBytes,rdmaReserveMiB:best.rdmaReserveMiB,targetRawUs:1e6/TARGET/MARGIN,remainingRawUs:1e6/TARGET/MARGIN-best.rawUs,services:best.services,protocol:protocols,ceilings:[{name:'实际候选',rawUs:best.rawUs},{name:'去掉等待',rawUs:best.computeUs+best.commUs},{name:'仅保留计算',rawUs:best.computeUs}].map(v=>({...v,tps:1e6/(v.rawUs*MARGIN)}))};}
function report(d){const b=d.extended.best,x=b.x,p=b.p,f=v=>Number(v).toLocaleString('en-US',{maximumFractionDigits:2}),esc=s=>String(s).replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]));const table=(h,rs)=>'<table><thead><tr>'+h.map(v=>'<th>'+esc(v)+'</th>').join('')+'</tr></thead><tbody>'+rs.map(r=>'<tr>'+r.map(v=>'<td>'+esc(v)+'</td>').join('')+'</tr>').join('')+'</tbody></table>';
 const arch=`<svg viewBox="0 0 1100 560"><style>text{font:14px sans-serif;fill:#172033}.b{fill:#fffdf8;stroke:#155e75;stroke-width:2}.a{stroke:#155e75;stroke-width:2;fill:none;marker-end:url(#m)}</style><defs><marker id="m" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6Z" fill="#155e75"/></marker></defs><rect class="b" x="35" y="20" width="1030" height="470" rx="12"/><text x="550" y="50" text-anchor="middle" font-size="22">单个 Compute Die：${f(p.dieArea)} mm² / ${f(p.diePower)} W</text><rect class="b" x="70" y="80" width="420" height="135"/><text x="280" y="108" text-anchor="middle">${x.nL} × L core</text><text x="280" y="140" text-anchor="middle">Matrix ${x.lEngines}×${x.lRows}×${x.lCols} · Vector ${x.vectorLanes}</text><text x="280" y="170" text-anchor="middle">TMA ${x.tmaEngines}×${x.tmaBytes} B/cycle</text><text x="280" y="198" text-anchor="middle">Local SRAM ${x.lMiB} MiB/core</text><rect class="b" x="610" y="80" width="420" height="135"/><text x="820" y="108" text-anchor="middle">${x.nH} × H core</text><text x="820" y="140" text-anchor="middle">Matrix ${x.hEngines}×${x.hRows}×${x.hCols} · Vector ${x.vectorLanes}</text><text x="820" y="170" text-anchor="middle">TMA ${x.tmaEngines}×${x.tmaBytes} B/cycle</text><text x="820" y="198" text-anchor="middle">Local SRAM ${x.hMiB} MiB/core</text><rect class="b" x="180" y="270" width="740" height="62"/><text x="550" y="296" text-anchor="middle">NoC ${x.nocLanes}×${x.nocBytes} B/cycle · ${f(p.nocTB)} TB/s</text><text x="550" y="320" text-anchor="middle">L/H core ↔ NoC ↔ Shared SRAM / Reduce</text><rect class="b" x="180" y="365" width="330" height="70"/><text x="345" y="394" text-anchor="middle">Shared SRAM ${x.sharedMiB} MiB</text><text x="345" y="418" text-anchor="middle">${f(p.sharedRead)}/${f(p.sharedWrite)} TB/s read/write</text><rect class="b" x="590" y="365" width="330" height="70"/><text x="755" y="394" text-anchor="middle">Shared Reduce</text><text x="755" y="418" text-anchor="middle">${x.reduceLanes} lanes · ${f(p.reduceTOP)} TOPS</text><path class="a" d="M280 215V270 M820 215V270 M550 332V365 M510 400H590"/><text x="550" y="468" text-anchor="middle">UCIe：Die↔MC + Die↔Die；RDMA NIC：${x.rdmaLanes} lanes</text><text x="550" y="515" text-anchor="middle">单卡 8 Die / TP=32；RDMA one-sided write 直接落入远端 SRAM mailbox</text></svg>`;
 const flow='<div class="flow">remote SRAM address → one-sided write → SRAM visibility/commit → ready notification → consumer acquire/read/reduce → ACK → epoch release</div>';
 const comm=b.protocol.map(q=>[q.name,q.count,q.phases,q.requests,f(q.wireBytes/1048576)+' MiB',f(q.timeUs)+' μs',f(q.workspace/1048576)+' MiB']);
 let h=`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><title>K3 B=1 · Direct RDMA-to-SRAM</title><style>body{margin:0;background:#f3f1eb;color:#172033;font:14px/1.6 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:1400px;margin:auto;padding:28px}h1{font-size:30px}.box{background:#fffdf8;border:1px solid #ccd5d8;border-radius:9px;padding:16px;margin:14px 0}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}.big{font-size:25px;color:#155e75;font-weight:700}.warn{background:#fff4d8;border-left:4px solid #b45309}.flow{font-family:monospace;background:#172033;color:#e8eef4;padding:18px;border-radius:7px;overflow:auto}table{width:100%;border-collapse:collapse;background:#fffdf8;margin:12px 0}th,td{text-align:left;padding:8px;border-bottom:1px solid #ddd}th{background:#eef4f5;position:sticky;top:0}svg{width:100%;height:auto}a{color:#155e75}@media(max-width:800px){.grid{grid-template-columns:1fr 1fr}}</style></head><body><main><p>2026-09-19 · TP=32 · B=1 · context=1,048,576 · 93层</p><h1>只搜索 1000 TPS/usr：RDMA 内存语义 + 直接读写远端 SRAM</h1><div class="box warn"><b>${d.extended.qualified?'搜索域内达到目标。':'本次搜索域未达到目标。'}</b> 1000 TPS/usr 要求端到端 1.000 ms/token；含 1.17 margin 时 raw ≤ ${f(d.diagnostics.targetRawUs)} μs。${d.extended.qualified?'':'这不是全架构空间不可能的证明。'}</div><div class="grid"><div class="box">最快 TPS<div class="big">${f(b.tps)}</div></div><div class="box">raw / e2e<div class="big">${f(b.rawUs)} / ${f(b.e2eUs)} μs</div></div><div class="box">SRAM 峰值 / 配置<div class="big">${f(b.peakReservedMiB)} / ${f(b.sramMiB)} MiB/Die</div></div><div class="box">RDMA receive reserve<div class="big">${f(b.rdmaReserveMiB)} MiB</div></div></div><h2>1. 建模的内存语义生命周期</h2><div class="box">${flow}<p>WRITE 不等于 READY；只有远端 SRAM 写入完成、commit/visibility 生效、ready 可见后，消费者才 acquire。消费者完成读取/归约后返回 ACK，所有 rank ACK 后 slot 才能按 epoch release，避免 ABA、重复写和提前复用。RDMA 仍受 issue、wire serialization、接收 SRAM 写口、NoC、reduce、WQE/CQE credit 与通知/ACK 延迟约束。</p></div><h2>2. 搜索与优选结果</h2>${table(['项目','结果'],[['搜索域可行候选',d.extended.rows.length],['尝试参数组',d.extended.attempts],['1000 TPS达标数',d.extended.qualified],['最佳 TPS/usr',f(b.tps)],['目标 raw预算',f(d.diagnostics.targetRawUs)+' μs'],['通信服务',f(b.commUs)+' μs'],['计算服务',f(b.computeUs)+' μs'],['等待',f(b.waitUs)+' μs'],['wire bytes',f(b.wireBytes/1048576)+' MiB'],['RDMA phases / requests',b.phases+' / '+b.requests]])}<h2>3. 单 Die 架构图</h2><div class="box">${arch}</div><h2>4. 各通信协议账</h2>${table(['通信算子','调用数','phase','requests','wire','耗时','mailbox/queue'],comm)}<h2>5. 最佳候选规格</h2>${table(['参数','值'],Object.entries(b.x).map(([k,v])=>[k,v]).concat([['die area',f(p.dieArea)+' mm²'],['die power',f(p.diePower)+' W'],['card power',f(p.cardPower)+' W'],['total SRAM',f(p.totalMiB)+' MiB/Die'],['local L/H',f(b.localL)+' / '+f(b.localH)+' MiB/core'],['backing',f(b.backingGB)+' GB/card'],['RDMA有效带宽',f(p.rdmaCardGB)+' GB/s/card']]))}<h2>6. 瓶颈与结论</h2><div class="box">${table(['服务项','μs'],Object.entries(b.services).map(([k,v])=>[k,f(v)]))}<p>当前瓶颈主要是 kernel、local TMA/端口以及 RDMA memory transport；direct-to-SRAM 消除了传统 collective 的部分额外拷贝/启动，但没有消除远端写可见性、ACK、卡内 Die 合并和 SRAM/NoC/reduce 资源竞争。若继续冲击 1000，应建模通信融合、批量 commit/notification、更多计算-通信重叠或减少同步 epoch，而不是把 RDMA 视为零成本。</p></div><p><a href="../../src/rdma/k3_b1_1000_rdma_sram_search.js">搜索程序</a> · <a href="../../data/rdma/k3_b1_1000_rdma_sram_results.json">完整结果 JSON</a> · <a href="../../src/rdma/k3_sram_memory_rdma_model.js">RDMA-SRAM 模型</a></p></main></body></html>`;return h;}
function run(){const t=Date.now(),scale=Number(process.env.B1_RDMA_SCALE||1);const old=JSON.parse(fs.readFileSync('data/search/k3_b1_1000_results.json'));const seeds=[A.BASE,...old.extended.rows.slice().sort((a,b)=>b.tps-a.tps).slice(0,16).map(r=>r.x)];const ext=search(EXT,seeds,{initial:96*scale,generations:4,offspring:32*scale,polish:3,seed:20260922});const d={version:'2026-09-19-rdma-sram',target:TARGET,batch:1,tp:32,margin:MARGIN,limits:A.LIMITS,tech:A.TECH,extended:ext,diagnostics:diagnostics(ext.best),seconds:(Date.now()-t)/1000};const hash=crypto.createHash('sha256');for(const f of ['src/rdma/k3_b1_1000_rdma_sram_search.js','src/rdma/k3_sram_memory_rdma_model.js','src/search/k3_architecture_search.js','src/simulation/k3_operator_sram_sim.js','src/core/design_engine.js'])hash.update(fs.readFileSync(f));d.inputHash=hash.digest('hex');fs.writeFileSync('data/rdma/k3_b1_1000_rdma_sram_results.json',JSON.stringify(d,null,2));fs.writeFileSync('reports/rdma/k3_b1_1000_rdma_sram_report.html',report(d));console.log(JSON.stringify({best:ext.best.tps,raw:ext.best.rawUs,qualified:ext.qualified,count:ext.rows.length,seconds:d.seconds},null,2));}
module.exports={EXT,evaluate,search,diagnostics,report};if(require.main===module)run();
